# Benchmarks — Cryoomega+Grok Bot vs Grok Bot alone

Automatic A/B yardstick runs (YS-AGI3 dims F/C/O + ops probes).

| Arm | Meaning |
|-----|---------|
| `cryoomega_mesh` | L0 Cryoomega + spektrum routing + routines/skills/profiles |
| `grok_bot_solo` | Baseline Grok Bot without mesh orchestration claims |

Results: `benchmarks/latest.json`, history under `benchmarks/history/`.
Pushed by routine **Benchmark Cryoomega vs Grok Bot**.
